# Devlog
Giko's Notes

### 1-27-25
- I agree, LaTeX generation is best
- Python Base + Automata Python Library (Explanation) -> Tikz + LaTex (Visual) -> Final Solution (Visual + Text)
- Setting up pygraphviz is in fact a pain ...
- If every problem can be solved via an algorithm, then the explanatory mathematical statements can be pregenerated for each step of the algorithm. Based on the problem , the pregenerated statements can be strung together to explain the problem in conjunction with the images? Additionally, if we tackle the problem via a transform and conquer approach, every opertion is predetermined and the mathematical statements can be pregenerated. 
