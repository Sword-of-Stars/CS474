\documentclass{article}
\usepackage{tikz}
\usetikzlibrary{automata, positioning}
\begin{document}
\begin{tikzpicture}[shorten >=1pt, node distance=3cm, on grid, auto]
\node[state, initial] (q0) at (0, 0) {$\mathsf{q0}$};
\node[state, accepting] (q_accept) at (3, 0) {$\mathsf{q_accept}$};
\path[->] (q0) edge node {$\mathsf{0 → 1, N}$} (q_accept);
\path[->] (q0) edge[loop above] node {$\mathsf{1 → 0, R}$} (q0);
\path[->] (q0) edge node {$\mathsf{_ → 1, N}$} (q_accept);
\end{tikzpicture}
\end{document}