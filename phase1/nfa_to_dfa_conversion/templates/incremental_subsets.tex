{% for state, closure in incremental.items() %}

\subsection*{Handling State {{state}}}

\begin{enumerate}
    \item \textcolor{gray}{Remove a set $S$ from $C$}
    We select the next set $S$ from $C$ to process. In this case, $S = \{ {{state}} \}$.    
    \item \textcolor{gray}{For each $a\in\Sigma$}} 
    \begin{enumerate}
        pass
    \end{enumerate}

\end{enumerate}

{% end for%}